import React from "react";

const Header: React.FC = () => {
  return (
    <header className="flex justify-between items-center">
      <div className="logo">Логотип</div>
      <nav>{/* Кнопки навигации */}</nav>
      {/* Секция профиля */}
    </header>
  );
};

export default Header;
